version: 1.3.5

cache: true

endpoints:
  custom:
    - name: "OpenAI Compatible"
      apiKey: "user_provided"
      baseURL: "user_provided"

      models:
        default:
          - "gpt-3.5-turbo"
        fetch: true

      titleConvo: true
      modelDisplayLabel: "OpenAI Compatible"
